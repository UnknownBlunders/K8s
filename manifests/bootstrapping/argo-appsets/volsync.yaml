apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: volsync
  namespace: argocd
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: volsync-system
  source:
    repoURL: "https://github.com/unknownblunders/K8s.git"
    path: manifests/bootstrapping/volsync-system
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - ServerSideApply=true
