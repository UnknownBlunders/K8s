apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kopiur
  namespace: argocd
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kopiur-system
  source:
    repoURL: "https://github.com/unknownblunders/K8s.git"
    path: manifests/bootstrapping/kopiur-system
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
