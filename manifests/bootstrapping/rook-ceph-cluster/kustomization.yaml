apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rook-ceph

helmCharts:
- name: rook-ceph-cluster
  releaseName: cluster
  namespace: rook-ceph
  repo: oci://ghcr.io/rook
  version: v1.19.5
  valuesFile: values.yaml

resources:
- dashboard-password-es.yaml
- grafanadashboard.yaml
